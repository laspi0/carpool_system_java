package com.groupeisi.rent.controllers.user;

import com.groupeisi.rent.Config.ViewUtils;
import javafx.fxml.FXML;
import javafx.stage.Stage;

public class AdminDashboardController {

    @FXML
    private void handleLogout() {
//        Stage currentStage = (Stage) ...; // Obtenez la référence de la scène actuelle si nécessaire
        // Logique pour se déconnecter (par exemple, retourner à la page de connexion)
    }

    // Autres méthodes et gestionnaires d'événements spécifiques au tableau de bord administrateur
}
